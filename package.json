{
  "name": "microboat-next-monorepo",
  "version": "1.0.0",
  "description": "Microboat Next.js Monorepo with Turborepo",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=@microboat/web",
    "deploy": "turbo deploy --filter=@microboat/web",
    "lint": "turbo lint",
    "format": "turbo format",
    "clean": "turbo clean",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "@biomejs/biome": "1.9.4",
    "typescript": "^5"
  }
}